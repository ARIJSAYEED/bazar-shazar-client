const HomePage = () => {
  return (
    <div>
      this is page contains the whole home page components
      <div>
        <button className="btn">home page</button>
      </div>
    </div>
  );
};

export default HomePage;
